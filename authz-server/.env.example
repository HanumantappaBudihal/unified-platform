# ===========================================
# AuthZ Server (OPA) - Environment Config
# ===========================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# OPA serves operator/infra-op authorization decisions. Identity (users, tokens)
# is owned by the Seiton Platform IdP — this server holds no identity config.
