# ===========================================
# Email Server (Mailpit) - Environment Config
# ===========================================

# Web UI credentials
MAILPIT_UI_USER=admin
MAILPIT_UI_PASSWORD=admin

# Ports
MAILPIT_SMTP_PORT=1025
MAILPIT_UI_PORT=8025

# Max messages to retain
MAILPIT_MAX_MESSAGES=1000
