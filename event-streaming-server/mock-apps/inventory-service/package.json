{
  "name": "inventory-service",
  "version": "1.0.0",
  "description": "Mock Inventory Service - manages stock and reacts to order events via Kafka Central",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "docker": "docker compose up -d"
  },
  "dependencies": {
    "kafkajs": "^2.2.4"
  }
}
