replicaCount: 3
resources:
  requests: { memory: "2Gi", cpu: "1" }
  limits: { memory: "4Gi", cpu: "2" }
storage:
  size: 200Gi
config:
  defaultReplicationFactor: 3
  minInsyncReplicas: 2
  logRetentionHours: 336
