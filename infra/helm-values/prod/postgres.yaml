resources:
  requests: { memory: "2Gi", cpu: "1" }
  limits: { memory: "4Gi", cpu: "2" }
storage:
  size: 200Gi
config:
  maxConnections: 500
  sharedBuffers: "1GB"
  effectiveCacheSize: "3GB"
backup:
  enabled: true
  schedule: "0 1 * * *"
  retentionDays: 90
